baseurl = "/"
languageCode = "en-us"
title = "Metro Developer"
theme = "hugo-future-imperfect"
preserveTaxonomyNames = true
paginate = 5
disqusShortname = ""
googleAnalytics = "UA-9140522-1"
pluralizeListTitles = false


[params]
  # Sets the meta tag description
  description          = "Metro's Official Site for Transit Data and Technology"
  # Sets the navbarTitle that appears in the top left of the navigation bar
  navbarTitle          = "Metro Developer"
  # Sets where "View More Posts" links
  viewMorePostLink     = "/blog/"

# Optional Params
  # Sets navbarTitle to match the section of the website
  dynamicTitles        = true
  # Sets RSS icons to appear on the sidebar with the social media buttons
  rssAppearAtTop       = true
  rssAppearAtBottom    = true
  # Sets Social Media icons to appear on the sidebar
  socialAppearAtTop    = true
  socialAppearAtBottom = true
  # Sets Categories to sort by number of posts instead of alphabetical
  categoriesByCount    = true
  # Sets Estimated Reading Time to appear in post headers
  includeReadingTime   = true
  # Sets the Favicon and Version for the site. Default support is for
  # apple-touch-icon-precomposed.png, favicon.png, favicon.ico, and mstile.png.
  # These are stored in the favicon folder. See the following for more information:
  # https://github.com/audreyr/favicon-cheat-sheet
  loadFavicon          = false
  faviconVersion       = ""
  # Sets Social Share links to appear on posts
  socialShare          = true
  # Load custom CSS or JavaScript files. The variable is an array so that you
  # can load multiple files if necessary. You can also load the standard theme
  # files by adding the value, "default".
  # customCSS            = ["default", "/path/to/file"]
  # customJS             = ["default", "/path/to/file"]
  customCSS            = ["default"]
  customJS             = ["default"]


# graphcomment
[params.graphcomment]
  # Sets GraphComment to be active
  graphcomment = true
  # Sets the location for Staticman to connect to
  shortname  = "metro-developer"


# Disqus will take priority over Staticman (github.com/eduardoboucas/staticman)
# due to its ease of use. Feel free to check out both and decide what you would
# prefer to use. See Staticman.yml for additional settings.
[params.staticman]
  # Sets Statiman to be active
  staticman = false
  # Sets the location for Staticman to connect to
  username  = ""
  repo      = ""
  branch    = ""

  [params.staticman.recaptcha]
    sitekey = "SITE_KEY"
    secret  = "ENCRYPTED_SECRET"

  # These are optional params related to the sidebar. They are recommended, but not
  # required for proper functionality. HTML is supported within the params.
[params.intro]
  header    = "Metro Developer"

  paragraph = "Technical information for Metro riders and data lovers. Over 10k subscribers!<i class='fa fa-hand-peace-o'></i>"

  about     = "Use this site to learn how to use Metro data in web & mobile applications."

  # This appears at the top of the sidebar above params.intro.header.
  # A width of less than 100px is recommended from a design perspective.
  [params.intro.pic]
    src       = "/img/main/logo-sm.jpg"
    # Sets Image to be a circle
    circle    = true
    # Sets Image to be Future Imperfect's hexagon
    imperfect = false
    width     = ""
    alt       = "Metro Developer"

[params.postAmount]
# Sets the number of recent posts to show in the sidebar. The default value is 5.
    sidebar = 5

# Sets the menu items in the navigation bar
# Identifier prepends a Font Awesome icon to the menu item
[[menu.main]]
  name = "Home"
  url = "/"
  identifier = "fa fa-home"
  weight = 1

[[menu.main]]
  name = "Blog"
  url = "/blog/"
  identifier = "fa fa-newspaper-o"
  weight = 3

[[menu.main]]
  name = "Resources"
  url = "/resources/"
  identifier = "fa fa-list"
  weight = 4

[[menu.main]]
  name = "Categories"
  url = "/categories/"
  identifier = "fa fa-sitemap"
  weight = 5

[[menu.main]]
  name = "Terms"
  url = "/terms/"
  identifier = "fa fa-id-card-o"
  weight = 2

[[menu.main]]
  name = "Contact"
  url = "/contact/"
  identifier = "fa fa-envelope-o"
  weight = 6

# Sets Social Media icons to appear and link to your account. Value should be your
# username unless otherwise noted. These are the icons affected by socialAppearAtTop
# and socialAppearAtBottom.
[social]
  # Coding Communities
  github          = "orgs/LACMTA/dashboard"
  gitlab          = "LACMTA/gtfs_bus"
  # Visual Art Communities
  flickr          = "metrolibraryarchive"
  # Publishing Communities
  medium          = ""
  # Social Networks
  facebook        = "losangelesmetro"
  youtube         = "channel/UC6mi3TVa60sTDgnuSNocbMw"
  twitter         = "metrolosangeles"
  # Email
  email           = "webmaster@metro.net"
